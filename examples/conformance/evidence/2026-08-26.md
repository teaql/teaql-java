# Java minimum runtime conformance evidence

Date: 2026-08-26

Model evaluation: 0 errors, 0 warnings, 0 suggestions, 8 solids.

```text
PASS ensureSchema (explicit SQLite DDL from Runtime Module)
PASS Checker (canonical title key, rejected before persistence)
PASS Create (id=1, version=1)
PASS Q API (typed SmartList<WorkItem>)
PASS E API (loaded, null fallback, and not-loaded are distinct)
PASS Update (version 1 -> 2)
PASS Delete (default Q excludes deleted rows)
PASS Java minimum runtime conformance: 7/7
```

The run discovered and corrected two real integration gaps: the Spring JDBC
provider lacked compiled row mapping, and the generated application installed
metadata but not generated Checkers into `TeaQLRuntime`.

The provider-specific regression passed 4/4. The full reactor was also run and
retains two pre-existing failures in `PortableSQLDatabaseTest`: the ascending
and descending continuous-page cases report `OFFSET_FALLBACK:CACHE_MISS`
instead of `CURSOR_SEEK`. Later modules are skipped by Maven after that failure;
this status is intentionally not represented as a green full-suite result.
